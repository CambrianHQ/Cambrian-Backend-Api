apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Fixed uid the provisioned dashboards reference (see dashboards' datasource uid).
    uid: cambrian-prometheus
    url: http://prometheus:9090
    isDefault: true
    editable: true
